[build]
  command = "npm ci --legacy-peer-deps && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# SPA routing (same effect as _redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
